[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "buscraft"
version = "0.1.0"
description = "BusCraft UVM VIP generator prototype"
requires-python = ">=3.9"
dependencies = [
    "PySide6>=6.6",
    "Jinja2>=3.1",
    "graphviz>=0.20",
]
